<%= form_with(model: plot, local: true) do |form| %>
  <% if plot.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(plot.errors.count, "error") %> prohibited this plot from being saved:</h2>

      <ul>
      <% plot.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :plot_code %>
    <%= form.text_field :plot_code, id: :plot_plot_code %>
  </div>

  <div class="field">
    <%= form.label :plot_name %>
    <%= form.text_field :plot_name, id: :plot_plot_name %>
  </div>

  <div class="field">
    <%= form.label :references %>
    <%= form.text_field :references, id: :plot_references %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
