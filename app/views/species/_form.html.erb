<%= form_with(model: species, local: true) do |form| %>
  <% if species.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(species.errors.count, "error") %> prohibited this species from being saved:</h2>

      <ul>
      <% species.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :species_code %>
    <%= form.text_field :species_code, id: :species_species_code %>
  </div>

  <div class="field">
    <%= form.label :species_name %>
    <%= form.text_field :species_name, id: :species_species_name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
